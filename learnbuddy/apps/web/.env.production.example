# learnbuddy Web — 生产构建（通常无需此文件）
# 复制为 apps/web/.env.production
#
# 官方 Web 默认直连 https://learnbuddy.ganzhibin.iuc，无需 VITE_GATEWAY_HTTP_URL。
# 仅自托管 / 同域反代时取消注释：

# VITE_USE_GATEWAY=true
# VITE_GATEWAY_HTTP_URL=/gateway-api
