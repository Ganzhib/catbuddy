{
  "name": "@catbuddy/gateway-workspace",
  "private": true,
  "version": "1.0.0",
  "description": "Web ⇄ Gateway ⇄ Desktop — session WS (monorepo)",
  "scripts": {
    "dev": "pnpm --filter @catbuddy/gateway dev",
    "build": "pnpm --filter @catbuddy/gateway build",
    "start": "pnpm --filter @catbuddy/gateway start",
    "lint": "pnpm -r lint",
    "test": "vitest run",
    "test:watch": "vitest",
    "test:e2e": "node test-e2e.mjs",
    "test:acceptance": "node test-acceptance.mjs",
    "test:all": "node test-all.mjs"
  },
  "devDependencies": {
    "dotenv": "^16.6.1",
    "typescript": "^5.7.2",
    "vitest": "^1.2.0",
    "ws": "^8.18.0"
  }
}
