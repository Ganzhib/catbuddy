# learnbuddy Gateway 独立配置
# 复制为 gateway/.env（勿提交 .env）
# 启动: docker compose up -d && pnpm run build && pnpm start

GATEWAY_PORT=18765
# 生产部署须与客户端内置 LEARNBUDDY_GATEWAY_DESKTOP_SECRET 一致
GATEWAY_SECRET=learnbuddy-desktop-pair-v1
# 独立域名 gateway.ganzhibin.icu 时用 /ws；仅 Vite 开发代理时用 /gateway-ws/ws
GATEWAY_WS_PATH=/ws
GATEWAY_DEV_WEB_TOKEN=dev-web

# 桌面 apps/desktop/.env.example → apps/desktop/.env
# GATEWAY_ACCOUNT_EMAIL 须与 Web 登录邮箱一致（App 登录后会自动同步）

# 必须开启邮箱登录时
GATEWAY_AUTH_REQUIRE_EMAIL=true
# E2E 测试 (pnpm gateway:test) 需要其一：DEV_BYPASS=true 或 REQUIRE_EMAIL=false
GATEWAY_AUTH_DEV_BYPASS=false
GATEWAY_JWT_SECRET=change-me-in-production

# MySQL（本机 pnpm gateway:dev 用 127.0.0.1；docker compose 会注入 MYSQL_HOST=mysql）
MYSQL_HOST=127.0.0.1
MYSQL_PORT=3306
MYSQL_USER=learnbuddy
MYSQL_PASSWORD=learnbuddy
MYSQL_DATABASE=learnbuddy_gateway
# 或使用连接串（二选一）:
# DATABASE_URL=mysql://learnbuddy:learnbuddy@127.0.0.1:3306/learnbuddy_gateway

# ── 注册验证码邮件（QQ 邮箱）──
# SMTP_PASS = QQ 邮箱「设置 → 账户 → POP3/SMTP」里的 **授权码**（不是 QQ 登录密码）
# 若 587 报 smtp_send_failed，请优先改用下面 465 + SSL 两行
SMTP_HOST=smtp.qq.com
SMTP_PORT=465
SMTP_SECURE=true
SMTP_USER=your-email@qq.com
SMTP_PASS=your-smtp-authorization-code
SMTP_FROM=your-email@qq.com

# 备选 STARTTLS（部分网络 587 不通）:
# SMTP_PORT=587
# SMTP_SECURE=false
