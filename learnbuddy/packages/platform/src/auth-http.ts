/** @deprecated Import from `./auth` or `@learnbuddy/platform` auth exports. */
export {
  BootstrapAuthRequired,
  clearAuthToken,
  hasAuthToken,
  loadAuthToken,
  requestEmailCode,
  requiresWebLogin,
  saveAuthToken,
  verifyEmailCode,
} from './auth'
